
В https://developer.github.com/v4/explorer/
сформировать graphql запрос который вернет:

query executeHomeWork($login: String = "aandolg", $countRepo: Int = 5, $countUser: Int = 5) {
  #Логин и имя пользователя
  user(login: $login) {
    login
    name
  }
  viewer {
    #Топ n репозиториев (их название и дату создания) 
    repositories(first: $countRepo, orderBy: {field: STARGAZERS, direction: DESC}) {
      nodes {
        name
        createdAt
      }
    }
    #Топ n пользователей за которыми вы следите (их имена) 
    following(first: $countUser) {
      nodes {
        name
      }
    }
  }
}


Query Variables
{
  "countRepo": 2,
  "login": "aandolg",
  "countUser": 5
}
